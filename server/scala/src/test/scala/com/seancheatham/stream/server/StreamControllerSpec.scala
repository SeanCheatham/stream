package com.seancheatham.stream.server

import org.scalatest.{Matchers, WordSpec}

class StreamControllerSpec extends WordSpec with Matchers {

  "A Stream Controller" must {
    "manage the start/stop functionality of a broadcast" in {
      assert(false)
    }
    "be able to add layers to a canvas" in {
      assert(false)
    }
    "be able to remove layers by name" in {
      assert(false)
    }

    "be able to remove layers by index" in {
      assert(false)
    }
  }

}
